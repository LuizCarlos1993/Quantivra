The project has two sources:

1. `/docs` → Functional and structural authority.
2. `/mockup` → Visual authority.

The generated application must be visually identical to `/mockup`
and structurally compliant with `/docs`.

No creative deviations allowed.
No UI improvisation allowed.