# BACKEND RULES

- All logic must be server-side
- No business logic in frontend
- Validation rules implemented in service layer
- Use database constraints where possible
- All writes logged in audit_logs
- Use transactions for batch validation